{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "jsx": "react-native",
    "module": "esnext",
    "target": "es2017",
    "outDir": "./build" // Si deseas que los archivos compilados se coloquen en una carpeta específica
  }
}
